# ============================================================
# CoachStage 后端依赖
# Python 3.11+
# ============================================================

# ---- Web 框架 ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# ---- 语音服务（Pipecat 实时语音 Pipeline）----
pipecat-ai[whisper,silero]>=0.2.0
aiohttp>=3.9.0

# ---- 数据库 ----
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# ---- Redis（redis>=5 已内置异步，aioredis 已废弃） ----
redis>=5.2.0

# ---- 配置管理 ----
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ---- HTTP 客户端（LLM、RLM、RAG 等通用调用） ----
httpx>=0.27.2

# ---- 钉钉集成 ----
dingtalk-stream>=0.24.3

# ---- LLM（OpenAI 兼容格式，底座可为 DeepSeek / 通义千问 / One-API） ----
openai>=1.0.0

